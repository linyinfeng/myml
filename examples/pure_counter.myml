import "../stdlib/fix.myml" ;;
import "../stdlib/integer.myml" ;;

Counter =
  let gen C r =
    class
      { get _ = r.x
      , inc _ = new (C (r update { x = integerPlus 1 r.x })) }
  in
    fix gen ;;
