import "fix.myml" ;;

put = putChar# ;;
get = getChar# ;;
compare = compareChar# ;;

eq c1 c2 = [ `LT _ = false, `EQ _ = true, `GT _ = false ] (compare c1 c2) ;;

char =
  { put = put
  , get = get
  , compare = compare
  , eq = eq
  } ;;
